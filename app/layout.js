import './globals.css';

export const metadata = { title: 'Blue Hour', description: 'Live vulnerability discovery & reporting' };

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
