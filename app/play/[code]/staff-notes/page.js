import QuickCart from '@/components/quickcart';import {flagFor,teamByCode} from '@/lib/db';import {notFound} from 'next/navigation';
export const dynamic='force-dynamic';
export default async function StaffNotes({params}){const t=await teamByCode(params.code);if(!t)return notFound();return <QuickCart code={t.join_code}><div className="qcmain card"><div className="eyebrow">Internal staff note</div><h1>Pre-launch checklist</h1><p>Debug credential issued for this storefront instance:</p><p className="flag">{await flagFor(t.id,'info_disclosure')}</p><p className="meta">Remember to remove internal notes before production.</p></div></QuickCart>}
