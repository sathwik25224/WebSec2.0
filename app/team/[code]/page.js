'use client';
import Link from 'next/link';import {useParams} from 'next/navigation';import {useState} from 'react';import {Timer,usePoll} from '@/components/live';
const types=['Information Disclosure','IDOR','Reflected XSS','Cookie Tampering','SQL Injection'];
export default function Team(){const {code}=useParams();const [data,refresh]=usePoll('/api/team?code='+code,null);const [selected,setSelected]=useState(null);if(!data)return <main className="shell">Loading team dashboard…</main>;const current=selected?data.challenges.find(x=>x.id===selected):null;return <main className="shell"><nav className="nav"><Link className="brand" href="/">BLUE HOUR</Link><div className="navlinks"><a target="_blank" href={'/play/'+data.team.joinCode}>QuickCart ↗</a><Link href="/leaderboard">Leaderboard</Link></div></nav><div className="dashboard-head"><div><div className="eyebrow">Team dashboard</div><h1 style={{margin:'7px 0'}}>Team: {data.team.name}</h1></div><div className="stats"><Timer game={data.game}/><div className="timer" style={{fontSize:'2.2rem'}}>Score: {data.score}</div></div></div><div className="grid">{data.challenges.map((c,i)=><button key={c.id} className={'card challenge '+(c.id===data.recommendedId?'recommended':'')} onClick={()=>setSelected(c.id)}><span className={'pill '+c.difficulty}>{c.difficulty}</span><div className="meta">CHALLENGE {i+1}{c.id===data.recommendedId&&' · RECOMMENDED NEXT'}</div><h3>{c.name}</h3><div className="mono">{c.base_points} points</div><div className={'status '+c.status.toLowerCase().replace(' ','-')}>{c.status}</div></button>)}</div>{current&&<Challenge key={current.id} challenge={current} code={data.team.joinCode} game={data.game} refresh={refresh} close={()=>setSelected(null)}/>}</main>}
function Challenge({challenge,code,game,refresh,close}){const [form,setForm]=useState({flag:'',vulnType:challenge.name,location:'',cause:'',impact:'',severity:'medium',reproduction:'',fixSuggestion:''});const [msg,setMsg]=useState(''),[busy,setBusy]=useState(false),[hint,setHint]=useState('');const locked=game.status!=='running';const set=(k,v)=>setForm({...form,[k]:v});async function submit(e){e.preventDefault();setBusy(true);setMsg('');const r=await fetch('/api/submit',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...form,vulnerabilityId:challenge.id})});const d=await r.json();setBusy(false);if(r.ok){setMsg({good:true,text:d.correct?(d.newlySolved?`✅ Correct! +${d.base} pts${d.firstBlood?' · ⚡ FIRST BLOOD +10':''}`:'✅ Correct — this challenge was already solved.'):'❌ Incorrect, try again'});refresh()}else setMsg({good:false,text:d.error});}async function getHint(tier){const r=await fetch('/api/hint',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({vulnerabilityId:challenge.id,tier})});const d=await r.json();if(r.ok){setHint(`Tier ${tier}: ${d.hint}`);refresh()}else setHint(d.error)}return <section className="card panel"><div className="dashboard-head"><div><div className="eyebrow">Challenge detail</div><h2>{challenge.name}</h2></div><button className="btn secondary" onClick={close}>Close panel</button></div><div className="two"><div><a className="btn" target="_blank" href={'/play/'+code}>Open QuickCart ↗</a><p className="meta">Investigate in a separate tab. Submit both your flag and a structured report here.</p><div className="actions"><button className="btn secondary" disabled={locked||challenge.hints.includes(1)} onClick={()=>getHint(1)}>Get Hint — Tier 1 · Cost: -10</button><button className="btn secondary" disabled={locked||challenge.hints.includes(2)} onClick={()=>getHint(2)}>Get Hint — Tier 2 · Cost: -10</button></div>{hint&&<div className="notice" style={{marginTop:12}}>{hint}</div>}{locked&&<div className="notice error" style={{marginTop:12}}>Submissions and hints unlock only while the organizer’s game timer is running.</div>}</div><form className={'join '+(msg?.good?'flash-good':msg?'flash-bad':'')} onSubmit={submit}><label>Flag<input className="mono" required value={form.flag} onChange={e=>set('flag',e.target.value)} placeholder="FLAG{...}"/></label><div className="formgrid"><label>Vulnerability Type<select required value={form.vulnType} onChange={e=>set('vulnType',e.target.value)}>{types.map(x=><option key={x}>{x}</option>)}</select></label><label>Severity<select required value={form.severity} onChange={e=>set('severity',e.target.value)}><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option></select></label><label className="wide">Location *<input required value={form.location} onChange={e=>set('location',e.target.value)} placeholder={'/play/'+code+'/…'}/></label><label className="wide">Cause *<textarea required value={form.cause} onChange={e=>set('cause',e.target.value)}/></label><label className="wide">Impact *<textarea required value={form.impact} onChange={e=>set('impact',e.target.value)}/></label><label className="wide">Reproduction *<textarea required value={form.reproduction} onChange={e=>set('reproduction',e.target.value)}/></label><label className="wide">Fix Suggestion <span className="meta">(optional)</span><textarea value={form.fixSuggestion} onChange={e=>set('fixSuggestion',e.target.value)}/></label></div>{msg&&<div className={'notice '+(msg.good?'success':'error')}>{msg.text}</div>}<button className="btn" disabled={busy||locked}>{busy?'Submitting…':'Submit Flag & Report'}</button></form></div></section>}
